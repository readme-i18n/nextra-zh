```ts
type Example = {
  /**
   * **Foo** description.
   * @default "dimaMachina"
   * @remarks `"not dimaMachina"`
   */
  foo: string
}
```

在此示例中，虽然属性 `foo` 的实际类型为 `string`，但根据 `@remarks` 标签的说明，其显示类型将为 `"not dimaMachina"`。

<APIDocs
  code={`
type Example = {
  /**
   * **Foo** description.
   * @default "dimaMachina"
   * @remarks \`"not dimaMachina"\`
   */
  foo: string
}
export default Example`}
/>