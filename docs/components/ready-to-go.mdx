### 渲染 MDX 文件

通过基于文件的路由系统，有两种方式可以渲染 MDX 文件：使用 [`page` 文件](/docs/file-conventions/page-file)或采用 [`content` 目录](/docs/file-conventions/content-directory)规范来添加您的 MDX 文件。

### 运行项目

执行 `package.json` 中指定的 `dev` 命令即可启动项目开发！🎉

```sh npm2yarn
npm run dev
```