import { Callout, Cards } from 'nextra/components'
import { MDXRemote } from 'nextra/mdx-remote'
import { createIndexPage } from 'nextra/page-map'
import { pageMap } from './[name]/page'

# API

<Callout type="info">
  本 API 参考文档由通配路由文件 `/api/[name]/page.tsx` 自动生成，使用了全新的 [Nextra `<TSDoc />` 组件](/docs/built-ins/tsdoc)。
</Callout>

<MDXRemote
  compiledSource={await createIndexPage(pageMap)}
  components={{
    Cards
  }}
/>