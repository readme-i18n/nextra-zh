---

asIndexPage: true
description:
  Nextra 文件规范指南详细说明了 Nextra 项目中文件和目录的结构与组织方式，包括 `page.mdx`、`_meta.js` 和 `mdx-components.js` 文件的使用，以及 `content` 和 `src` 目录的配置。
---

import { FileIcon, FolderIcon, MdxIcon } from 'nextra/icons'
import { OverviewPage } from '../../_components/overview-page'

# 文件规范

<OverviewPage
  filePath={metadata.filePath}
  icons={{ FileIcon, FolderIcon, MdxIcon }}
/>