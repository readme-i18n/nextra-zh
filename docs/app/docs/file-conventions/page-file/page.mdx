---

icon: FileIcon
sidebarTitle: page.mdx
description:
  Nextra 中的 `page.mdx` 文件是一种特殊的 Next.js App Router 约定文件，
  允许您为路由定义专属 UI。默认情况下，`page` 可使用 `.js`、`.jsx` 或 `.tsx` 扩展名，
  而 Nextra 额外支持 `.md` 和 `.mdx` 扩展名。
---

import { FileTree } from 'nextra/components'

# `page.mdx` 文件

[`page` 文件](https://nextjs.org/docs/app/api-reference/file-conventions/page) 是
[Next.js App Router 的约定文件](https://nextjs.org/docs/app/getting-started/project-structure#routing-files)，
用于为路由定义专属 UI：

<FileTree>
  <FileTree.Folder name="app" open>
    <FileTree.File name="layout.jsx" />
    <FileTree.File name="page.jsx" active />
    <FileTree.Folder name="docs" open>
      <FileTree.File name="page.mdx" active />
      <FileTree.Folder name="getting-started" open>
        <FileTree.File name="page.mdx" active />
      </FileTree.Folder>
    </FileTree.Folder>
  </FileTree.Folder>
</FileTree>

> [!TIP]
>
> 默认情况下，`page` 可使用 `.js`、`.jsx` 或 `.tsx` 文件扩展名。<br/>
> Nextra 额外支持 `.md` 和 `.mdx` 扩展名。