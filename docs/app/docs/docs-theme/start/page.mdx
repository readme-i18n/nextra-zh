---

sidebarTitle: Get Started
icon: ChevronRightIcon
---

import InstallNextraTheme from '@components/install-nextra-theme.mdx'
import ReadyToGo from '@components/ready-to-go.mdx'

import { Steps } from 'nextra/components'

# 文档主题

Nextra 文档主题是一个几乎包含构建现代文档网站所需全部功能的主题，它包含：

- 顶部导航栏
- 搜索栏
- 页面侧边栏
- 目录(TOC)
- 以及其他内置组件

> [!TIP]
>
> 本网站就是使用 Nextra 文档主题构建的。

## 作为新项目启动

<Steps>
### 安装

要手动创建 Nextra Docs 站点，您需要安装 **Next.js**、**React**、**Nextra** 和 **Nextra Docs Theme**。在项目目录中运行以下命令安装依赖项：

```sh npm2yarn
npm i next react react-dom nextra nextra-theme-docs
```

> [!NOTE]
>
> 如果项目中已安装 Next.js，只需安装 `nextra` 和 `nextra-theme-docs` 作为附加组件即可。

<InstallNextraTheme />

```jsx filename="app/layout.jsx"
import { Footer, Layout, Navbar } from 'nextra-theme-docs'
import { Banner, Head } from 'nextra/components'
import { getPageMap } from 'nextra/page-map'
import 'nextra-theme-docs/style.css'

export const metadata = {
  // 在此定义元数据
  // 有关元数据API的更多信息，请参阅：https://nextjs.org/docs/app/building-your-application/optimizing/metadata
}

const banner = <Banner storageKey="some-key">Nextra 4.0 已发布 🎉</Banner>
const navbar = (
  <Navbar
    logo={<b>Nextra</b>}
    // ... 您的其他导航栏选项
  />
)
const footer = <Footer>MIT {new Date().getFullYear()} © Nextra.</Footer>

export default async function RootLayout({ children }) {
  return (
    <html
      // 非必需，但对SEO有益
      lang="en"
      // 必须设置
      dir="ltr"
      // 由 `next-themes` 包建议 https://github.com/pacocoursey/next-themes#with-app
      suppressHydrationWarning
    >
      <Head
      // ... 您的其他头部选项
      >
        {/* 其他标签应作为 `<Head>` 元素的 `children` 传递 */}
      </Head>
      <body>
        <Layout
          banner={banner}
          navbar={navbar}
          pageMap={await getPageMap()}
          docsRepositoryBase="https://github.com/shuding/nextra/tree/main/docs"
          footer={footer}
          // ... 您的其他布局选项
        >
          {children}
        </Layout>
      </body>
    </html>
  )
}
```

<ReadyToGo />

</Steps>

请参阅[文件约定](/docs/file-conventions)了解如何组织文档结构，并查看[布局组件](/docs/docs-theme/built-ins/layout)来配置文档站点的主题。